[package]
name = "llm-gateway-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "llm-gateway のコマンドライン (serve / auth / status)"

[[bin]]
name = "llm-gateway"
path = "src/main.rs"

[dependencies]
llm-gateway = { path = "../llm-gateway", version = "0.14" }
llm-gateway-server = { path = "../llm-gateway-server", version = "0.14" }

tokio = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
axum = { workspace = true }
# usage は server の HTTP 越しに取る (CLI 単独では持たない、DR-0007)。
reqwest = { workspace = true }
